import styles from "./styles.module.css";

const Topbar = () => {
  return (
    <div className={styles.topbar}>
      <div className={styles.topbarTitle}>React Test</div>
    </div>
  );
};

export default Topbar;
